set et           # Use a space character when a tab key is used
set ts=2         # Indent 2 spaces when tabbing
set ai           # Indent at the same level as the precious line
set bg=dark      # Assume dark background
set cursorcolumn # Highlight columns
set nu           # Show line numbers
syntax on        # Syntax highlighting